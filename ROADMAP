
0.3: due by the end of july 2010 ? Before releasing, we need to add:
  * full MPI support
  * full OpenMP support (including parallel section, single section, 'team ids', etc.)

0.4: due by the end of august 2010 ? Before releasing, we need to:
  * use the GTG library for trace writing

0.5: due by the end of september 2010 ? Before releasing, we need to:
  * add BLAS support

1.0: due by the end of 2010 ?
 * generic function interception
 * add OpenCL and CUDA support
 * add support for performance counters
